# A105B

This repository contains a minimal project. The `telegram_adapter/` directory holds a sample Python polling adapter to connect Telegram to `clawd.bot`.

See `telegram_adapter/README.md` for usage details.
